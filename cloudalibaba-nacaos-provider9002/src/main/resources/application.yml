server:
  port: 9002
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848



#服务暴露配置
management:
  endpoints:
    web:
      exposure:
        include: '*'